{
  "name": "roamdx",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -C apps/server dev",
    "build": "pnpm -C apps/server build",
    "start": "pnpm -C apps/server start",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "node-pty", "sharp"]
  }
}
